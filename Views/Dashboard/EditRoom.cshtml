@model Rooms
@{
}

<div class="container">
    <div class="alert alert-success"> Update the data </div>
    <form asp-action="UpdateRoom" method="post">
        @* <input type="hidden" asp-for="@Model.IdHotel" /> *@
        <div class="row">
            <div class="col">
                <label for="Id" class="p-2">رقم الغرفة  </label>
                <input type="number" id="Id" name="Id" class="form-control form-control-sm" asp-for="@Model.Id" readonly required />
                <span class="text-danger" asp-validation-for="Id"></span> <!-- Because hotel in model not in inumerable-->
                
                    @* <div class="col">
                        <label for="Name" class="p-2">اختر الفندق</label>
                        <select class="form-control form-control-sm" name="IdHotel">
                            <!-- Notice this IdHotel linked to bring same Id in models the id is linked with hotel module-->
                        
                            @foreach (var item in ViewBag.Hotel)
                            {
                                <option value="@item.Id">@item.Name</option>
                            }
                        </select>
                    </div> *@
            </div>

            <div class="col">
                

                    <label for="IdHotel" class="p-2">اختر الفندق</label>
                <select class="form-control form-control-sm" id="IdHotel" name="IdHotel" asp-for="@Model.IdHotel" required>
                    <!-- Notice this IdHotel linked to bring same Id in models the id is linked with hotel module  asp-for="IdHotel"-->
                        @foreach (var item in ViewBag.Hotel)
                {
                        <option value="@item.Id"> @item.Id | @item.Name </option>
                }
                </select>
                @* <label for="IdHotel" class="p-2">رقم الفندق  </label>
                <input type="number" id="IdHotel" name="IdHotel" class="form-control form-control-sm" asp-for="@Model.IdHotel" readonly /> *@
                <span class="text-danger" asp-validation-for="IdHotel"></span> <!-- Because hotel in model not in inumerable-->
                <div class="col">

                    @* <label for="IdHotel" class="p-2">اختر الفندق</label>
                    <select class="form-control form-control-sm" id="IdHotel" name="IdHotel" asp-for="@Model.IdHotel">
                    <!-- Notice this IdHotel linked to bring same Id in models the id is linked with hotel module-->
                    @foreach (var item in Model.IdHotel)
                    {
                    <option value="@Model.IdHotel">@Model.IdHotel</option>
                    }
                    </select>

                    <label for="Name" class="p-2">اختر الفندق</label>
                    <select class="form-control form-control-sm" name="IdHotel">
                    <option value="0"></option>
                    @foreach(var item in ViewBag.Hotel)
                    {
                    <option value="@item.Id">@item.Name</option>
                    }
                    </select>
                    *@
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-6">
                <label for="Type" class="p-2"> Room Type </label>
                <input type="text" id="Type" name="Type" class="form-control form-control-sm" asp-for="@Model.Type" required />
                <span class="text-danger" asp-validation-for="Type"></span> <!-- Because hotel in model not in inumerable-->

            </div>
            <div class="col-sm-6">
                <label for="Price" class="p-2">  سعر </label>
                <input type="text" id="Price" name="Price" class="form-control form-control-sm" asp-for="@Model.Price" required />
                <span class="text-danger" asp-validation-for="Price"></span> <!-- Because hotel in model not in inumerable-->

            </div>
        </div>

        
        <div class="row">
            <div class="col">
                <label for="Images" class="p-2"> صورة </label>
                <input type="text" id="Images" name="Images" class="form-control form-control-sm" asp-for="@Model.Images" required />
                <span class="text-danger" asp-validation-for="Images"></span>
            </div>

        </div>

        <div class="row mt-3">
            <div class="col text-center">

                <button class="btn btn-success" type="submit">حفظ</button>
            

            </div>

        </div>
    </form>

</div>


